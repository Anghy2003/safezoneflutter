<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.safezone_app">

    <!-- 🌐 INTERNET -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 📍 UBICACIÓN (geolocator / mapas SafeZone) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 🎥 CÁMARA (image_picker) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 🎙 MICRÓFONO (record) 👉 FALTABA -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- 🔔 NOTIFICACIONES (Android 13+) 👉 FALTABA para Firebase Messaging -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 💾 ALMACENAMIENTO (opcionales / puedes quitarlos si no los usas directo) -->
    <!--
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    -->

    <!-- 🔥 SERVICIO EN 1ER PLANO (SOS / ubicación) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- 📳 VIBRACIÓN PARA ALERTAS -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 💤 EVITAR QUE EL TELÉFONO DUERMA DURANTE ALERTAS -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 🔁 INICIAR SERVICIOS DESPUÉS DE REINICIO (si alguna vez lo usas) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="SafeZone"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- ACTIVIDAD PRINCIPAL -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Flutter engine -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- 🚨 Servicio SOS en 1er plano -->
        <service
            android:name=".VolumeService"
            android:exported="false"
            android:foregroundServiceType="location" />

    </application>

    <!-- 👇 Permite consultar ciertas intents externas (no es obligatorio tocar esto) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
